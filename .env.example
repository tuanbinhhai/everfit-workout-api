# Server
PORT=3000
LOG_LEVEL=info

# Database (used from Step 2 onward — see docs/ARCHITECTURE.md)
DATABASE_URL=postgresql://workout:workout@localhost:5432/workout_api?schema=public

# Pagination / bulk limits (docs/CLARIFICATIONS.md #16)
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
MAX_BULK_ENTRIES=100
